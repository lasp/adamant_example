; This script runs at startup automatically

; Run the autocoded generate packet pages script to set up all the pages for packets in the assembly
start linux_example_packet_pages

; Run the setup script
start setup
